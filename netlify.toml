 [build]
   command = "npm run build"
   publish = "dist"
 
[build.environment]
  NODE_VERSION = "22.12.0"

 # React Router (SPA) fallback so deep links work
 [[redirects]]
   from = "/*"
   to = "/index.html"
   status = 200
